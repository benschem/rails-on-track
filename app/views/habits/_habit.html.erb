<div id="<%= dom_id habit %>">
  <div class="border-2 border-black <%= habit.done_today ? "bg-red" : "bg-green" %>">
    <h3>🏆 <%= habit.hot_record %></h3>
    <h2><%= habit.name %></h2>
    <h3>
      <% if habit.hot_streak > habit.cold_streak %>
        <%= "🔥 #{habit.hot_streak}" %>
      <% else %>
        <%= "🚫 #{habit.cold_streak}" %>
      <% end %>
    </h3>
    <% days_ago = (Date.today - habit.date_last_done).to_i %>
    <h4>Last done: <%= days_ago %> <%= days_ago == 1 ? 'day' : 'days' %> ago.</h4>
  </div>
  <% if action_name != "show" %>
    <%= link_to "Show this habit", habit, class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to 'Edit this habit', edit_habit_path(habit), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
    <hr class="mt-6">
  <% end %>
</div>
